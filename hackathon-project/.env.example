# Postgres (Docker)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/soundglobe

# Replicate (server-only)
REPLICATE_API_TOKEN=

# Anthropic (server-only)
ANTHROPIC_API_KEY=

# Mapbox (public — used in browser)
NEXT_PUBLIC_MAPBOX_TOKEN=
